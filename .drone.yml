kind: pipeline
name: default

workspace:
  base: /account 
  path: src/github.com/sanjitverma/account
steps:
  - name: build
    image: maven:3.6.2-jdk-11-slim
    commands:
      - mvn package -DskipTests=false
